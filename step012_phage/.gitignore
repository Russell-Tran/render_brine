# Rebuilt by `make structure` in about 6 seconds, and 74 MB of it. The builder
# and the deposited structures are what belong in the repo, not the output.
Resources/scene.json
# The deposited coordinate files, refetched by the builder from RCSB.
Resources/*.cif
Resources/*.pdb
.build/
renders/
